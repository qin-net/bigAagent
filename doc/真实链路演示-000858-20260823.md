# 首次研究真实链路演示（给经管）

> 用途：给经管看「系统怎么跑、偏好怎么进分析、最后出什么报告」。  
> **研究辅助，不是投资建议。** 数字来自当次 AKShare 快照，口径可能与年报/券商研报不一致，请当过程样例看，不要当荐股。

| 项 | 值 |
|---|---|
| 标的 | 五粮液 / 000858 |
| 时间 | 2026-08-23 约 16:51–16:52（UTC 08:51–08:52） |
| 耗时 | 约 57 秒（拉数约 28 秒 + 四专家并行约 29 秒） |
| 模型 | DeepSeek `deepseek-v4-flash`，thinking 开启 |
| 数据 | 实时 AKShare，**不是** fixture |
| run_id | `258d74c2-5793-40f3-8356-347a19cc2fd1` |
| 用户输入 | `#remember #fundamental 估值必须对照经营现金流与自由现金流，不要只看PE` |

---

## 1. 这条链路在演示什么

用户给股票代码 + 一段偏好。系统**不选股**，只按偏好分析这只已指定的票。

本次偏好：基本面估值要对照经营现金流 / 自由现金流，不要只看 PE。带 `#remember`，短句会入库；下次再研 000858 基本面会自动带上（本次是第一次写入）。

---

## 2. 分析过程（按真实发生顺序）

### 2.1 编排拆用户意图（约 1.4 秒）

程序识别 tag：`#remember`、`#fundamental`。  
再调一次 LLM 抽槽（不进专家 System、不存用户原话）。

落盘意图：

| 槽 | 值 |
|---|---|
| 基本面 | 对照经营现金流与自由现金流进行估值，不要只看PE |
| 技术 / 情绪 / 宏观 / 决策 / 追踪 | `none` |

效力 `remember`。偏好表写入一条：`scope=fundamental`，`stock_code=000858`，statement 同上。

### 2.2 拉四份快照（AKShare）

审计 `snapshot_ready`。基本面规则机算（不是模型猜的）：

- 触发 `valuation_cheap`：PE 约 21.93，近 5 年分位 3.6；PB 约 2.16，分位 0.8
- 触发 `cashflow_lag`：利润同比 +82.57%，经营现金流 **-25.3537**（单位随数据源，此处按快照原值）
- 未过 `roe_quality`：ROE 6.5（未达规则里的 15）
- 负债率 34.31，未触发高杠杆

现价快照：**71.19**。

### 2.3 四个分析 Agent 并行（互不通信）

各有独立 session，只调本维工具，最后 `submit_final` 交结构化报告。

| 角色 | 主要工具 | 结果 |
|---|---|---|
| 基本面 | `get_fundamental_snapshot`、`search_methodology` | hold / 3 分 |
| 技术面 | 指标 / K 线 / 价格快照、方法论 | sell / 2 分 |
| 情绪面 | 公告事件、股东变动、方法论 | buy / 4 分 |
| 宏观 | 利率快照 | **弃权**（与白酒相关度 low） |

宏观弃权：**计入已使用、不缺维、不把整次 run 打成失败**。不参与买卖投票。

### 2.4 系统决策（不是第五个 Agent）

模板综合三维投票 + 宏观背景。本次：

- 基本面 hold 3 → `value_score=3`
- 技术面 sell 2 → `timing_score=2`
- 情绪 buy，与基本面/技术面分歧写入 `disagreements`
- 综合 **hold**，confidence **0.56**

证伪条件主要来自基本面可检验句（现金流能否转正、ROE 门槛、估值分位是否还「便宜」）。

---

## 3. 分维报告（正文摘要）

### 基本面（hold，3）— 偏好有体现在结论里

估值处于近 5 年极低分位，但盈利质量有警示：净利 80.63、利润同比很高，经营现金流为负，命中 `cashflow_lag`。ROE 未过质量门槛。负债表相对稳健。

**按用户口径：** 低估值要用现金流转正来验证，不能只凭低 PE；快照没有自由现金流字段，已写入 `missing_information`。

一句话原文：

> 五粮液估值处于近5年极低分位：PE 21.92985107（5年分位 3.6）、PB 2.15889734（5年分位 0.8），触发 valuation_cheap；但盈利质量存在显著警示——净利 80.63、利润同比 +82.57%，经营现金流却为 -25.3537，触发 cashflow_lag 规则，利润增长缺乏现金流支撑；ROE 6.5 低于 15 的质量门槛（roe_quality 未通过）。资产负债表稳健（负债率 34.31、流动比率 2.56、未触发 high_leverage）。按用户口径对照经营现金流估值：低估值需以现金流转正验证，不能仅凭低 PE 判定，故给予 hold、3 分。

### 技术面（sell，2）

现价低于 MA5/10/20/60，短中期空头排列；MACD 为负且柱走阔；RSI14 约 19.5 超卖，只当作短线反弹风险，不当作趋势反转。

关键位：近期高点 78.84；近期低点 71.11；MA20 74.80；MA60 74.37。

### 情绪面（buy，4）

近 60 个自然日窗口内：控股股东增持进展、回购进展、分红实施、半年报预告。窗口内无减持/负面公告。预告正文未披露，列为缺失信息。群体风险 `medium`。

### 宏观（abstain，相关度 low）

1Y LPR 3.0%、5Y 以上 3.5%；隔夜 Shibor 缺失。判断白酒经营对利率不是一阶变量，故弃权、不给方向。

---

## 4. 综合报告（给用户看的终稿）

| 项 | 值 |
|---|---|
| 评级 | **hold** |
| 价值分 / 时机分 | 3 / 5 ， 2 / 5 |
| 置信度 | 0.56 |
| 已用维度 | 基本面、技术面、情绪、宏观（宏观弃权但仍算 used） |
| 缺维 | 无 |

**理由（系统拼接，非模型另写长文）：**  
基本面 hold、score=3。技术面 sell、score=2。情绪面 buy。宏观：有利率数据，相关性 low，本维不形成方向。

**分歧：**  
基本面 hold vs 技术面 sell；基本面 hold vs 情绪 buy；技术面 sell vs 基本面 hold。

**一句话建议：**  
价值面 3/5，时机面 2/5，综合 hold；fundamental 为 hold，technical 为 sell。

**主要风险（来自基本面）：**

- 经营现金流为负与利润高增长背离，现金含量低  
- ROE 未过质量门槛  
- 快照无 FCF / 商誉 / 非经常损益占比  

**可检验证伪（摘录）：**

- 后续季报经营现金流转正并覆盖净利 → 滞后警报可解除；连续两期仍为负 → 质量恶化确认  
- 年报 ROE 回到 15 以上且负债率仍低于 60，或持续低于 15  
- PE 五年分位升破 30，或低分位叠加现金流持续为负（价值陷阱）

---

## 5. 建议经管看的点（不是让改代码）

1. **偏好是否用对：** 基本面明确写了「按用户口径对照经营现金流」「不能仅凭低 PE」；缺 FCF 也说了。技术/情绪没有被这句话带跑。  
2. **三维打架是否可接受：** 价值 hold、走势 sell、事件 buy，综合 hold、置信度压到 0.56。这是设计如此，不是跑挂了。  
3. **宏观对白酒弃权** 是否符合你们对「宏观角色」的预期。  
4. **数据口径：** ROE 6.5、经营现金流为负等，请金融侧核对是季报/TTM/单位问题，还是规则该调。系统只保证「引用快照字段」，不保证会计定义与年报一致。  
5. **记忆范围：** 这次偏好绑在 000858 基本面，不是按偏好去选下一只股票。

完整机器 JSON：`data/e2e-jinguan/runs/258d74c2-5793-40f3-8356-347a19cc2fd1.json`（本地，未进 git）。
